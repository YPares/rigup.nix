# rigup configuration
# Define your AI agent rigs here

[rigs.default.riglets]
# Include riglets from this repository
# self = [ ... ]
# Include riglets from external inputs
rigup = ["agent-rig-system"]

# Configuration for the riglets (if they define options)
# [rigs.default.config.some-option]
# value = "example"

# Local overrides:
# When using the `rigup` CLI tool, you can create a gitignored rigup.local.toml
# to override rigup.toml without modifying it:
# - Riglet lists are concatenated
# - Config values are deeply merged
# - Extends values are replaced
